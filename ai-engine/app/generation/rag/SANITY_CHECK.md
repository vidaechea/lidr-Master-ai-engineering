# RAG Sanity Check

Checklist:

- Package imports resolve from `app.generation.rag.*`
- API embedding router imports from `app.generation.rag` modules
- Existing endpoints remain unchanged
- Legacy chunking/embedder implementations remain available during transition
